[project]
name = "fluxmo"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "js>=1.0",
    "webtypy>=0.1.7",
]

[dependency-groups]
dev = ["workers-py"]
