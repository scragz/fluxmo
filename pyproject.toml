[project]
name = "fluxmo"
version = "0.1.0"
description = "FLUX preset builder."
readme = "README.md"
requires-python = "~=3.12.0"
dependencies = ["webtypy>=0.1.7"]

[dependency-groups]
dev = ["workers-py", "js>=1.0"]
